// boolean (true / false)
let isOpen: boolean
isOpen = true

// string ('foo', "foo", `foo`)
let message: string
message = `isOpen: ${isOpen}`

//number (int, float, hexa, binario)
let total: number
total = 3
total = 40.99
total = 0xff0

// array (type[])
let items: number[]
items = [1, 2, 3, 4]

let letters: Array<string>
letters = ['a', 'b', 'c']